use crate::renderer::color::*;

pub struct Framebuffer {
    pub width: u32,
    pub height: u32,
    pub pixels: Vec<Color>,
}

impl Framebuffer{
    //fill
    //square
    //triangle
}
